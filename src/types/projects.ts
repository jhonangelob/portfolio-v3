export type ProjectTypes = {
  title: string;
  subtitle: string;
  description: string;
  repository: string;
  link: string;
  technologies: string[];
};

export type ProjectsTypes = { data: ProjectTypes[] };
